import { db, collection, query, where, getDocs, limit } from './firebase.js';
import { safeText, money, statusLabel, formatDate, $ } from './ui.js';
const params = new URLSearchParams(location.search); const initial = params.get('codigo')||''; if($('codeInput')) $('codeInput').value=initial;
$('validateForm')?.addEventListener('submit', e=>{e.preventDefault(); validar($('codeInput').value.trim());}); if(initial) validar(initial);
async function validar(code){ const el=$('validateResult'); if(!code){el.innerHTML='<p class="empty">Digite um codigo.</p>'; return;} el.innerHTML='<p>Consultando...</p>'; const snap=await getDocs(query(collection(db,'fretes'), where('code','==',code), limit(1))); if(snap.empty){ el.innerHTML='<article class="item"><h3>Codigo nao encontrado</h3><p>Confira o codigo informado e tente novamente.</p></article>'; return; } const d=snap.docs[0]; const f={id:d.id,...d.data()}; el.innerHTML=`<article class="item"><div class="item-header"><div><h3>Codigo valido: ${safeText(f.code)}</h3><p>Frete intermediado pela plataforma FreteHub.</p></div><span class="badge open">Valido</span></div><div class="modal-grid"><article><span>Status</span><strong>${statusLabel(f.status)}</strong></article><article><span>Criado em</span><strong>${formatDate(f.createdAt)}</strong></article><article><span>Rota</span><strong>${safeText(f.originText)} → ${safeText(f.destinationText)}</strong></article><article><span>Valor</span><strong>${money(f.price)}</strong></article><article><span>Motorista autorizado</span><strong>${safeText(f.selectedDriverName,'Aguardando liberacao')}</strong></article><article><span>Contratacao</span><strong>${f.contratacaoConfirmada?'Confirmada':'Pendente'}</strong></article></div><p class="hint">Esta consulta mostra dados limitados para validacao, sem expor endereco ou contatos privados.</p><div class="actions"><a class="button-link" href="acompanhar.html?codigo=${encodeURIComponent(f.code)}">Acompanhar status</a></div></article>`; }
